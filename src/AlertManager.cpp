#include "AlertManager.hpp"

namespace guardian {

// Implementation can be mostly empty for now as it's a structural requirement
// or it can implement the on_detection logic if not in the header.
// Looking at the header, on_detection was already implemented inline.
// However, CMake expects the file. I'll move the implementation here.

} // namespace guardian
